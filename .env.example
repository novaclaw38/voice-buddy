# Optional: set your OpenRouter API key at build time.
# If left blank, the parent enters it in the app's Settings tab instead.
# WARNING: If set here, it will be bundled into the public JS — only use this
# for self-hosted private deployments.
VITE_OPENROUTER_KEY=
